#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to secrets.h and enter your local Wi-Fi credentials.
// secrets.h is ignored by Git and should not be committed.
const char* WIFI_SSID = "YOUR_WIFI_SSID";
const char* WIFI_PASS = "YOUR_WIFI_PASSWORD";

#endif // SECRETS_H
